<ul id="medium-photos-list"
  data-controller="medium-photos-manager"
  <% if @archive_item.persisted? %>
    data-medium-photos-manager-url-value="<%= update_medium_photos_order_archive_item_path(@archive_item) %>"
    data-medium-photos-manager-upload-url-value="<%= upload_medium_photos_archive_item_path(@archive_item) %>"
  <% end %>
>
  <% if @archive_item.persisted? && @archive_item.medium_photos.attached? %>
    <% @archive_item.ordered_medium_photos.each do |file| %>
      <li data-id="<%= file.blob.id %>" class="preview-thumbs">
        <% if file.content_type == "image/jpeg" || file.content_type == "image/png" %>
          <div class="removable-image-container">
            <%= link_to 'X Remove File', delete_medium_photo_archive_item_path(@archive_item, medium_photo_id: file.id), method: :delete, data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, class: "clear_one_attachment_button" %>
            <%= image_tag file.variant(resize_to_limit: [600, nil]), class: "admin-form-image" %>
          </div>
        <% else %>
          <div class="no-thumbnail-content-file">
            <%= link_to 'View photo', file, target: "_blank", class: "download-link" %>
            <%= link_to 'X Remove File', delete_medium_photo_archive_item_path(@archive_item, medium_photo_id: file.id), method: :delete, data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, class: "delete-file-link" %>
          </div>
        <% end %>
      </li>
    <% end %>
  <% end %>

  <%= hidden_field_tag "archive_item[medium_photos_order]", "", id: "medium-photos-order" %>
  <%= file_field_tag "archive_item[medium_photos][]", multiple: true, id: "new-medium-photos-input", accept: "image/*", style: "display:none;" %>

  <div class="mp-dropzone">
    Drop photos here or click to upload
  </div>
</ul>